class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        if len(s) != len(t):
            return False
        
        set1 = sorted(s)  # Sorting characters of string s
        set2 = sorted(t)  # Sorting characters of string t
        
        return set1 == set2
